document.getElementById("text-end").addEventListener("click", function () {
    // get textarea
    const textArea = getTextArea("textarea-field");

    // set text align right
    textArea.style.textAlign = "right";
});
